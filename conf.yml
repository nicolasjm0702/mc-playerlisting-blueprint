info:
  name: "Player Listing"
  identifier: "playerlisting"
  description: "Shows connected/max Minecraft players below Network (Outbound) on the server console."
  version: "1.0"
  target: "beta-2026-01"
  author: "nicolas"

admin:
  view: "admin/view.blade.php"

dashboard:
  components: "components"

requests:
  app: "controllers"
  routers:
    client: "routes/client.php"
